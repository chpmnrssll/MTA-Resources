The FPS Stats resource counts the Frames per Second (FPS) and displays them on the screen. It also shows the last x seconds in a graph.

To install, just put it into your resources folder. You may change the defaultSettings.lua file if you want to change the default settings.